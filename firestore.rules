service cloud.firestore {
  match /databases/{database}/documents {
    match /users/{userid} {
      allow read, update, delete, create: if request.auth.uid != null && request.auth.uid == userid;
    }
  }
}
